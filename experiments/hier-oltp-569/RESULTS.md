# Замер: иерархический OLTP на квартетах (issue #569)

Дата: 2026-09-10. Окружение: Windows 11, 12 логических ядер, 16 ГБ RAM, NVMe;
PostgreSQL 16.4 (портативный), `shared_buffers=2GB`, `work_mem=256MB`, `fsync=off`
(влияет только на загрузку, не на чтение). Без партиций.

Данные: 999 990 транзакций × 90 дней, 54 реквизита (45 заполнено), генерация `gen.py`
(seed 569). Раскладки и загрузка — `load.sql`, запросы — `bench.sql`, `bench2.sql`.
Тайминги ниже — тёплый (второй) прогон; холодный местами хуже в 2–7 раз (отмечено).

## Строки и размеры

| Раскладка | Строк | Групп | Зап./группу | Таблица | Индексы | Всего |
|---|---:|---:|---:|---:|---:|---:|
| wide (54 колонки) | 1,0 млн | — | — | 390 MB | 67 MB | 458 MB |
| flat | 45 999 540 | — | — | 2814 MB | 3081 MB | 5896 MB |
| hier2 (дата+филиал) | 44 013 060 | 4 500 | 222,2 | 2692 MB | 2965 MB | 5658 MB |
| hier4 (+валюта+тип) | 42 511 695 | 102 423 | 9,8 | 2602 MB | 2883 MB | 5486 MB |
| hier8 (+ISO,ПК,POS,статус) | 46 514 844 | 946 136 | 1,1 | 2891 MB | 3185 MB | 6077 MB |

## Запросы (тёплые)

| Запрос | wide | flat | hier2 | hier4 | hier4+накопители |
|---|---:|---:|---:|---:|---:|
| Q1 сумма по филиалам, 7 дн | 0,31 с | 2,71 с | 9,32 с | 9,55 с | 0,79 с |
| Q1b счёт по филиалам, 7 дн | — | 2,61 с | 0,05 с | 0,19 с¹ | — |
| Q2 счёт по типам, 30 дн | 1,25 с | 10,46 с | — | 3,58 с | — |
| Q3 сумма по валютам, 90 дн | 1,27 с | 28,26 с | — | 40,10 с | 4,67 с |
| Q4 точечный по PAN + карточка | 3 мс | 3 мс | — | 3 мс | — |

¹ холодный прогон Q1b hier4 — 1,39 с.

Накопитель (реквизит t=2001 «сумма детей» на разрезе hier4): одноразовая сборка 27 с;
в проде — инкремент при вставке.

## Выводы (кратко)

1. Правило частотности обязательно: hier8 при 1,1 зап./группу хуже плоского и по строкам (+1%), и по байтам (+3%).
2. Агрегаты по размерностям разреза — кратный выигрыш (счёт по филиалам 53× на hier2; по типам 2,9× на hier4).
3. Меры нижнего уровня иерархия замедляет (Q1 3,5×; Q3 1,4×) — лечится накопителями на разрезе (3–6× быстрее плоского), ценой инкремента на вставку.
4. Точечный OLTP-доступ одинаков во всех квартетных раскладках.
5. Широкая таблица остаётся на порядок компактнее и быстрее простых агрегатов — иерархия сокращает разрыв, не отменяя его.

Оговорки: один корпус, одна машина, синтетика с независимыми полями (реальные корреляции
уменьшают число групп → выигрыш растёт), без партиций, 100 вопросов масштаба нет —
абсолютные числа на 703 млн будут другими, проверять там.

## Дозамер 10.09: профиль Ф110 (habr neoflex 451218)

Форма (отчётная дата, точность) → коды (код, сумма₽, сумма$). 4470 дат, 200–600 кодов
на форму, 1 788 392 показателя. Генерация `gen_f110.py`, загрузка/замер `f110.sql`+`f110q.sql`.

| | f_flat | f_hier |
|---|---:|---:|
| Строк | 8 941 960 (5,00/показатель) | 5 374 116 (3,00/показатель, −39,9%) |
| Размер (таблица+индексы) | 579+595=1174 MB | 345+387=731 MB (−37,7%) |
| Q1full сумма ₽ по датам, весь корпус (f110q2.sql) | 4,26 с | 7,76 с (проигрыш 1,8×) |
| Q1y сумма ₽ по датам, год 2010 (~8% корпуса) | 1,05 с | 0,47 с (выигрыш 2,2×) |
| Q2 динамика кода по датам | 2 мс | 1 мс |
| Q3 показатели одной даты | 2 мс | 2 мс |

⚠️ Первая версия дозамера содержала ошибку: Q1 с `ORDER BY дата DESC LIMIT 3` иерархический
план исполнял потоково (3 формы, 73 мс), а плоский — целиком (860 мс); «12×» был артефактом.
Честный протокол — полная агрегация без LIMIT (`f110q2.sql`), числа в таблице выше.

Вывод: экономия хранения = доля вынесенных полей × (1 − 1/группа); диапазон 7% (транзакция,
4/46 полей, группа 9,8) … 40% (Ф110, 2/5 полей, группа ~400). Меры нижнего уровня:
на селективном диапазоне ёмкие группы дают выигрыш (2,2× на годе), на полном скане путь
в два прыжка проигрывает (1,8×) — для полного охвата нужны накопители на разрезе.
